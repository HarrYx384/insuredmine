pipeline{

    agent any
    stages{
        stage("sourcecode checkout from git-hub"){
            steps{
            checkout ([$class: 'GitSCM', branches: [[name: '*/main']], extensions: [], userRemoteConfigs: [[url: 'https://github.com/HarrYx384/insuredmine.git']]])
        }
        }
    }   
}
